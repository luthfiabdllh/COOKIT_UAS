<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Warna saat ditekan -->
    <item android:color="@color/md_red_900" android:state_pressed="true" />
    <!-- Warna saat dinonaktifkan -->
    <item android:color="@color/md_grey_600" android:state_enabled="false" />
    <!-- Warna default -->
    <item android:color="@color/md_red_600" />
</selector>
