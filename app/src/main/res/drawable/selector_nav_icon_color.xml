<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Warna untuk item yang dipilih (aktif) -->
    <item android:color="@color/white" android:state_checked="true" />
    <!-- Warna untuk item yang tidak dipilih (nonaktif) -->
    <item android:color="@color/md_teal_400" android:state_checked="false" />
</selector>